# Dependencies
node_modules/

# Package lock (regenerated on install)
package-lock.json

# Documentation
docs/

# Build/serve files
serve.cjs
# ownspace.xpi is tracked on purpose: CI publishes it to main so Firefox can
# fetch it over HTTPS as the update channel (see gecko.update_url).
ownspace.xpi

# Local backlog
backlog.md

# OS files
.DS_Store
Thumbs.db